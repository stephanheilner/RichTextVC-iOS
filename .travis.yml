language: objective-c
osx_image: xcode8
before_install:
- gem install cocoapods -v '1.1.0.rc2.2' # Since Travis is not always on latest version
script:
- set -o pipefail && xcodebuild test -workspace Example/RichTextVC-iOS.xcworkspace -scheme RichTextVC-iOS-Example -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO | xcpretty
- pod lib lint
